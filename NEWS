?.?.?
  * Fix timeouts longer than 60 seconds
  * RPCError.translate() maps regular expressions to better exceptions
  * RtorrentRPC.get_supported_method() returns first supported method from a
    sequence of candidates
  * RtorrentRPC.multicall() makes "system.multicall" requests more convenient
  * QbittorrentRPC.disconnect().: Doesn't raise ConnectionError (other
    exceptions are still raised)


1.0.0
  * QbittorrentRPC.call(): "data" argument has been renamed to "options"


0.1.0
  * Initial release
